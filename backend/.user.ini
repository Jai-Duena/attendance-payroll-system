; Per-directory PHP configuration for the backend API folder.
; On shared hosting (e.g. InfinityFree with PHP-FPM), this file overrides
; any auto_prepend_file / auto_append_file set by the host, preventing HTML
; ad-injection from corrupting JSON API responses.
auto_prepend_file = ""
auto_append_file  = ""
